json.extract! article, :id, :ref_key, :author, :string, :journal, :, :title, :volume, :number, :pages, :year, :month, :note, :page_id, :created_at, :updated_at
json.url article_url(article, format: :json)
